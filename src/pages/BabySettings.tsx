import { BabyConfig } from "@/components/BabyConfig";

const BabySettings = () => {
  return (
    <div className="min-h-screen bg-gradient-soft">
      <div className="container mx-auto py-8">
        <BabyConfig />
      </div>
    </div>
  );
};

export default BabySettings;