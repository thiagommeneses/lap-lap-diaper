import { PageSettingsConfig } from "@/components/PageSettingsConfig";

const PageSettings = () => {
  return (
    <div className="min-h-screen bg-gradient-soft">
      <div className="container mx-auto py-8">
        <PageSettingsConfig />
      </div>
    </div>
  );
};

export default PageSettings;